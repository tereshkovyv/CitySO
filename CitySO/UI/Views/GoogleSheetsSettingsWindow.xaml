<Window x:Class="CitySO.UI.Views.GoogleSheetsSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Настройки подключения" Height="340" Width="520"
        WindowStartupLocation="CenterOwner"
        Background="#0F172A">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Подключение к Google Таблицам" FontSize="18" FontWeight="Bold" Foreground="#60A5FA" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" Text="JSON-ключ" Foreground="#94A3B8" FontSize="12" Margin="0,0,0,8"/>
        <TextBox Grid.Row="2" Text="{Binding JsonKey, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="120" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 Background="#1E293B" Foreground="#F1F5F9" BorderBrush="#334155" BorderThickness="1" Padding="10"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,12,0,0">
            <Button Content="Выполнить вход" Command="{Binding AuthorizeCommand}" Background="#34D399" Foreground="#0F172A" Padding="12,6" FontSize="13" BorderThickness="0" Width="160" Height="38"/>
            <Button Content="Выйти" Command="{Binding LogOutCommand}" Background="red" Foreground="#0F172A" Padding="12,6" FontSize="13" BorderThickness="0" Width="160" Height="38"/>
        </StackPanel>
        <TextBlock Grid.Row="4"  Text="{Binding LoginStatus}" Foreground="#F1F5F9" FontSize="13" VerticalAlignment="Center" Margin="16,0,0,0"/>
    </Grid>
</Window>
